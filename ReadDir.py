import sys, os 

path="dirParent"

"""
try:
    with os.scandir(path) as entries:
        for entry in entries:
            print(entry.name)
except:
    print("no se puede encontrar el directorio")
"""

